spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/txndb
    username: txn
    password: txn
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
